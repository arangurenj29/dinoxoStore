---
interface Props {
  alt: string;
  name:
    | 'gift-card'
    | 'help'
    | 'instagram'
    | 'instant-delivery'
    | 'linktree'
    | 'payment'
    | 'region'
    | 'secure-purchase'
    | 'tiktok'
    | 'usa-flag'
    | 'whatsapp-contact';
}

const { alt, name } = Astro.props;
---

<span
  class:list={['icon', `icon--${name}`]}
  role={alt ? 'img' : undefined}
  aria-label={alt || undefined}
  aria-hidden={alt ? undefined : 'true'}></span>
